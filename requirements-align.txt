# 可选：按假名（モーラ）反馈。装了才会出现「按假名对齐这篇」按钮和逐字反馈。
# 会额外占用约 2GB 磁盘，第一次运行还要下载约 1.2GB 的 MMS 对齐模型。
# 不装完全不影响其它功能。
#   pip install -r requirements-align.txt
torch>=2.1
torchaudio>=2.1
pykakasi>=2.2

# 调型词典（模式② 的目标高低、模式③ 的目标台阶）。词典首次运行时下载约 23MB，解压后 107MB。
# 内含 NAIST 日本語辞書（3-clause BSD）和 Open JTalk（Modified BSD）。
pyopenjtalk>=0.4
